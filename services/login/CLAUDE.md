# CLAUDE.md for login-service

This file provides guidance to Claude Code when working with the login service.

## Commands

### `login-service` (Python - FastAPI)
- **Install:** `pip install -r requirements.txt`
- **Start:** `uvicorn main:app --reload --port 8001`
