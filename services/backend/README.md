# Personal Finance Service

This is the main backend for the Personal Finance Manager application. It is a Spring Boot application written in Java 17.

It provides REST APIs for managing transactions, categories, and users. It uses an in-memory H2 database for data storage.
