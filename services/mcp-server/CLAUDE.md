# CLAUDE.md for personal-finance-mcp-server

This file provides guidance to Claude Code when working with the MCP server.

## Commands

### `personal-finance-mcp-server` (MCP Server - Python)
- **Install:** `pip install -r requirements.txt`
- **Start:** `uvicorn app.main:app --reload`
